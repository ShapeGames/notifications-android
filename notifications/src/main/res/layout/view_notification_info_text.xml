<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="dk.shape.games.notifications.presentation.viewmodels.settings.NotificationInfoViewModel.Text" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/bellIcon"
            android:layout_width="11dp"
            android:layout_height="11dp"
            android:src="@drawable/ic_notifications_bell_black"
            android:tint="#252525"
            android:layout_marginTop="3.5dp"
            app:layout_constraintVertical_bias="0"
            app:layout_constraintBottom_toBottomOf="@id/notificationTypes"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@id/notificationTypes" />

        <TextView
            android:id="@+id/notificationTypes"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="sans-serif-medium"
            android:lineSpacingMultiplier="1.1"
            android:maxLines="2"
            android:paddingHorizontal="5dp"
            android:text="@{viewModel.text}"
            android:textColor="#7d7d7d"
            android:textSize="12sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0"
            app:layout_constraintStart_toEndOf="@id/bellIcon"
            tools:text="So end Reals, GaReals, Game s, Ganlt, Goals, Game start, PenaReals, Game s, Ganlt, Goals, Game start, PenaReals, Game s, Ganlt, Goals, Game start, Pename s, Ganlt, Goals, Game start, Penalty" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
