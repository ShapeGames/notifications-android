<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="dk.shape.games.notifications.presentation.viewmodels.settings.NotificationInfoViewModel.Text" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/bellIcon"
            android:layout_width="11dp"
            android:layout_height="11dp"
            android:src="@drawable/ic_notifications_bell_black"
            android:tint="#252525"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/notificationTypes"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="sans-serif-medium"
            android:lineSpacingExtra="1.5sp"
            android:maxLines="2"
            android:paddingHorizontal="5dp"
            android:text="@{viewModel.text}"
            android:textColor="#7d7d7d"
            android:textSize="12sp"
            app:layout_constraintBottom_toBottomOf="@id/bellIcon"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0"
            app:layout_constraintStart_toEndOf="@id/bellIcon"
            app:layout_constraintTop_toTopOf="@id/bellIcon"
            tools:text="End Resusult, Goals, Game start, Pensult, Goals, Game start, Penlt, Goals, Game start, Penalty" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
